import {View} from 'react-native';
import React from 'react';

const Gap = ({width, height}) => {
  return (
    <View style={{width: width, height: height, backgroundColor: '#DFDFE0'}} />
  );
};

export default Gap;
